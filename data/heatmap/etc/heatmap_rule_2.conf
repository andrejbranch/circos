<rule>
condition=var(value) > 0.3 && var(ebov) > 0.3 && var(sudv) > 0.3 && var(marv) > 0.3
fill_color = red
</rule>

<rule>
condition=var(value) > 0.3 && var(ebov) > 0.3 && var(sudv) > 0.3
fill_color = orange
</rule>

<rule>
condition= var(value) > 0.3
condition= var(ebov) > 0.3
fill_color = green
</rule>

<rule>
condition=var(value) > 0.3
fill_color = blue
</rule>
